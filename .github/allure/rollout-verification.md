# Allure rollout verification

This temporary pull request exists only to produce a fresh `Run tests` workflow and validate the released Allure reporting chain. It contains no product-code changes and will be closed without merging after the report artifact and Pages publication are verified.
