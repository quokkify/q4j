name: Verify toolkit reusable Java CI
on:
  push:
    branches: [verify/toolkit-18c54f1]
  workflow_dispatch:
jobs:
  toolkit:
    uses: quokkify/project-toolkit/.github/workflows/java-ci.yml@18c54f1387565734745c4a2b58aac6c06060dfc6
    with:
      working-directory: .
      java-version: "21"
      build-tool: gradle
      lint-command: bash ./gradlew classes --no-daemon
      test-command: bash ./gradlew test --no-daemon
      build-command: bash ./gradlew assemble --no-daemon
